git tag v1.0

git tag -a v1.0 -m "Release version 1.0"

git add .
git commit -m "Final changes before v1.0 release"

git push origin v1.0

git push origin --tags

git tag

git show v1.0

git tag -d v1.0


git push origin --delete v1.0
